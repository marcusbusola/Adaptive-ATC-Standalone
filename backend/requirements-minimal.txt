# Minimal requirements to run the WebSocket server
# Use this if full requirements.txt fails to install on Python 3.14

# Core Web Framework
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0

# Database & Storage (optional for basic WebSocket testing)
# sqlalchemy>=2.0.0
# aiosqlite>=0.19.0

# Data Validation & Processing
python-dotenv>=1.0.0

# HTTP & API
httpx>=0.25.0
aiofiles>=23.0.0
python-dateutil>=2.8.0

# Logging
loguru>=0.7.0

# Note: ML dependencies (scikit-learn, tensorflow, keras) are excluded
# as they don't build on Python 3.14 yet. The server will run without them
# but ML-based features (Condition 3) will not work.
